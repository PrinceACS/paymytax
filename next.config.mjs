/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['randomuser.me',
          'img.freepik.com',
      'img.freepik.com',
      'images.unsplash.com'

        ],
      }
};

export default nextConfig;
